//Commented out because of SFML2 transition
/*
#pragma once
#include <SFML/Graphics.hpp>

class Animation
{
    public:
        Animation();
        void init(sf::Image& img, int framenum, float time, int sx = 0, int sy = 0);
        sf::Sprite& Getspr();
        void Setspace(float val) { frametime = val; }
        void SetX(float x) {sheet.SetX(x);};
        void SetY(float y) {sheet.SetY(y);};
        void SetCenter(float w, float h) {sheet.SetCenter(w, h);};
        void SetRotation(float rot) {sheet.SetRotation(rot);};
    private:
        sf::Sprite sheet;
        sf::Clock timer;
        float frametime;
        int curx;
        int cury;
        int stx;
        int sty;
        int curframe;
        int frames;
};
*/
